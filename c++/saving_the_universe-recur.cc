#include <iostream>
#include <vector>
#include <string>
using namespace std;

int s_eng[] = {0, 1, 2};
int s_query[] = {1, 2, 1, 1, 1, 1, 0, 0, 0, 0, 2, 0};
int n = 3, q = 12;

int
find_min(int s, int cur)
{
        
int
main () {
        for (int i(0); i < n; i++)
                cout << find_min (s_eng[0], 0);
        return 0;
}
